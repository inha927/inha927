# AI 협업 워크플로 설계 — 검토·판정 기록

> 2026-08-16 · v2.0 (Codex 통합 반영) · 규칙의 정본은 루트 `AGENTS.md` — 이 문서는 **왜 그렇게 정했는지**의 기록이다

## 3계층 모델 (유지)

| 계층 | 성격 | 위치 | 공통/구분 |
|---|---|---|---|
| **방법** (협업 규칙·커맨드) | 거의 안 변함 | `AGENTS.md` + `.claude/commands/` | **공통** |
| **지식** (UEFN·Verse 검증 사실) | 천천히 변함 | claude.ai 스킬 등 | **공통** |
| **상태** (진행·수치·결정·테스트) | 매일 변함 | **각 프로젝트 루트 `DEV_STATE.md`** | **프로젝트별. 절대 섞지 않는다** |

## v2.0에서 바뀐 것 — Codex 통합 판정표

2026-08-16, 사용자가 Codex(GPT) 정리본을 전달했다. 조항 단위 판정:

| 항목 | Claude v1.0 | Codex | 판정 |
|---|---|---|---|
| 플라이 이스케이프에 DEV_STATE.md | ❌ 금지 (인수인계 문서가 정본) | ✅ 생성 완료, 인수인계는 역사 보관 | **Codex 채택.** 현장이 이미 전환됐고 사용자가 승인했다. 시작 로드 96KB→21.9KB 경량화 효과도 실재 [확인: Codex 보고]. 단서: PASS/FAIL 정본은 테스트 기록 유지, DEV_STATE 게이트표는 사본 표기 |
| 시작 읽기 | 4문서 (AGENTS→색인→인수인계→테스트기록) | 2파일 (AGENTS+DEV_STATE), 나머지 온디맨드 | **Codex 채택.** 토큰 절약 목적에 부합 |
| 크기 기준 | "한 화면" | DEV_STATE 8KiB · AGENTS 24KiB · 합계 32KiB 예산 | **Codex 채택.** 실행 가능한 수치라서 |
| 프로젝트 형태 | 정본형/단일형 2형태 | 전 프로젝트 단일 구조 | **Codex 채택 + Claude 보완.** Docs/ 심층 계층은 유지 (스펙·테스트 기록·역사) |
| 사실/추측 표기, 폐기 목록, 잠금, 민감정보 금지, 사용자 승인 게이트, 한국어, UEFN 기준 | 있음 | 요약본에 없음 | **Claude 유지.** 충돌 아님 — Codex 요약이 다루지 않은 영역 |
| 원본-사본 관리 | 없음 | 없음 (사본을 만들기만 함) | **신설.** 사본이 4곳 생겼으니 버전 대조 규칙 필요 (부록 B) |
| 앵커 단일 규칙 | 없음 | "공통 일지 안 만든다"까지만 | **신설.** FREEZE TAG 분기 사고(아래)가 근거 |
| 표준 시작 문구 | /resume 커맨드 | 자연어 한 줄 | **둘 다.** 문구는 모든 AI 공용, /resume은 Claude용 실행형 |

Codex 보고 중 직접 검증 못 한 것: 17/17 검증, 파일 크기, Verse 해시 `32E1C11E…`, Epic 권장 문서 링크 — 전부 [확인: Codex 보고] 지위. PC에서 재확인 가능.

## ⚠️ FREEZE TAG 앵커 분기 사고 (2026-08-16 발견)

상태 앵커 후보 2개가 서로 모순된다:

| 앵커 | 내용 | 문제 |
|---|---|---|
| claude.ai 스킬 `project-state.md` | v3.1 + **v3.2 (2026-08-08 사용자 확정)**: 난입 자유형·1인 최소·버튼 4개·눈덩이/돌진 채택 | Drive 쪽을 모른다 |
| Drive `FREEZETAG_04_프로젝트현황_LIVE_v4.md` (2026-08-08) | 스스로 "최신 앵커, 01~03 폐기" 선언. **매일 06:00 KST 자동 작업이 이걸 읽는다.** 오류 정정 12건·작업량 283h 분석 등 v3.2에 없는 가치 보유 | **v3.2 사용자 확정 사항 미반영** (6~12인 유지, 4번째 버튼 폐기, 봇 연습 모드 유지 — v3.2와 정반대) |

어느 쪽이 나중인지 [미확인]. **판정 전까지 어느 쪽도 정본 취급 금지.** 매일 06:00 자동 작업이 v3.2 미반영 앵커로 돌고 있다는 게 당장의 운영 리스크다.

권고 (사용자 판정 필요): v3.2(사용자 확정)를 규칙의 기준으로 삼고, v4.0의 오류 정정·작업량 분석을 그 위에 병합한 v5 앵커를 **한 곳**에 만든 뒤, 나머지에 이관 표지를 남긴다. 06:00 자동 작업의 참조 대상도 그때 교체.

지식 문서 중복 (충돌 아님, 통합 후보): 스킬 `uefn-knowledge.md` ↔ Drive `UEFN_Master_Reference.md`(08-12).

## 기기·계정 전역 적용 지도

| 경로 | 적용 범위 | 상태 |
|---|---|---|
| repo `AGENTS.md` **(원본)** | 레포를 여는 모든 AI·기기 | ✅ v2.0 |
| `%USERPROFILE%\.codex\AGENTS.md` | Codex 전역 | ⚠️ Codex가 자체 작성한 구버전 — **v2.0으로 교체 필요** |
| PC `%USERPROFILE%\.claude\` | 그 PC의 Claude Code 전역 | ⏳ 설치 대기 (아래) |
| claude.ai 개인 설정 한 줄 | 계정 전역 | ⏳ 붙여넣기 대기 |
| 각 프로젝트 루트 | 그 프로젝트 | 플라이 이스케이프 ✅ [확인: Codex 보고] |

### PC 1회 설치 (Windows)

```bat
mkdir %USERPROFILE%\.claude\commands
:: 이 레포의 .claude\commands\dev-state.md, resume.md 를 위 폴더로 복사
:: 이 레포의 AGENTS.md 전문을 %USERPROFILE%\.claude\CLAUDE.md 에 붙여넣기 (또는 @경로 참조)
```

claude.ai **설정 → 프로필 → 개인 선호** 한 줄:

> 프로젝트 작업 시 github.com/inha927/inha927 의 AGENTS.md 공통 프로토콜(v2.0)을 따른다. 핵심: 프로젝트 루트 AGENTS.md+DEV_STATE.md 두 파일로 시작, 종료 시 DEV_STATE 갱신, 실행 안 한 검증은 PASS 금지, 상태 앵커는 프로젝트당 1개.

## 클라우드 세션의 한계 [확인: 이 세션 실측]

플라이 이스케이프 파일은 UEFN Revision Control로만 동기화되므로 클라우드 Claude는 읽을 수 없다 (아티팩트 스냅숏만 가능). 필요해지면 `Docs/`만 private 레포 미러 — 현재는 불필요해서 안 만들었다.

## 변경 이력

- **2026-08-16 v2.0** Codex 통합. v1.0의 "정본형에 DEV_STATE 금지" 판정 철회 (판정표 참조). FREEZE TAG 앵커 분기 사고 기록.
- **2026-08-16 v1.0** 최초 작성 (3계층 모델, 아티팩트=대시보드 판정).
