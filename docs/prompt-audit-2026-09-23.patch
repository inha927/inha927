diff --git a/.claude/commands/dev-state.md b/.claude/commands/dev-state.md
index dc15156..fecb537 100644
--- a/.claude/commands/dev-state.md
+++ b/.claude/commands/dev-state.md
@@ -22,7 +22,7 @@ allowed-tools: Read, Write, Edit, Glob, Grep, Bash(git status:*), Bash(git diff:
 
 ```markdown
 # DEV_STATE
-> 갱신: YYYY-MM-DD HH:MM · 갱신자: <AI 이름> · (git이면) 브랜치
+> 갱신: YYYY-MM-DD HH:MM · 갱신자: <AI 이름/사람> · (git이면) 브랜치
 ## 지금 목표
 ## 완료된 것            (검증 방법까지)
 ## 핵심 결정과 이유      (왜 — 이게 제일 가치 있다)
@@ -49,4 +49,4 @@ allowed-tools: Read, Write, Edit, Glob, Grep, Bash(git status:*), Bash(git diff:
 
 ## 5. 마지막
 
-무엇을 추가·삭제·수정했는지, 파일 크기와 함께 3줄로 보고한다.
+무엇을 추가·삭제·수정했는지와 갱신 후 파일 크기를 짧게 보고한다.
diff --git a/.claude/commands/resume.md b/.claude/commands/resume.md
index d9b41e5..af8e671 100644
--- a/.claude/commands/resume.md
+++ b/.claude/commands/resume.md
@@ -18,7 +18,7 @@ allowed-tools: Read, Glob, Grep, Bash(git status:*), Bash(git log:*), Bash(git b
 
 ## 그다음
 
-1. 3줄로 요약한다: 어디까지 됐나 / 왜 이 방식인가 / 다음 할 일
+1. 짧게 요약한다: 어디까지 됐나 / 왜 이 방식인가 / 다음 할 일
 2. **"다음 할 일" 첫 항목부터 시작한다.** 큰 결정(공통 프로토콜 기준: 게임 규칙 변경, 일정·목적 변경 등)이 끼어 있으면 그것만 묻고, 아니면 진행한다
 3. **"실패한 접근"·"폐기" 목록의 방법은 재시도 금지.** 되살릴 이유가 생겼다면 그 이유부터 사용자에게
 4. 작업을 마치면 `/dev-state`로 세션을 닫는다
diff --git a/AGENTS.md b/AGENTS.md
index 79adfdd..cc0803a 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -116,5 +116,4 @@
 
 ## 변경 이력
 
-- **v2.0 (2026-08-16)** Codex 전역 설정과 통합. 정본형/단일형 2형태 구분을 폐지하고 표준 구조로 단일화 — 플라이 이스케이프에도 `DEV_STATE.md`가 재개 진입점이 됐다 (v1.0의 "정본형에 DEV_STATE 금지" 판정 철회. 사유: Codex가 인수인계를 역사 보관으로 정리하고 사용자가 승인). 크기 예산(8KiB/24KiB)·표준 시작 문구·원본-사본 버전 대조·앵커 단일 규칙 신설.
-- **v1.0 (2026-08-16)** Claude 초안.
+버전별 변경 내용과 조항별 판정 근거는 `docs/ai-workflow-architecture.md`에 있다.
diff --git a/docs/ai-workflow-architecture.md b/docs/ai-workflow-architecture.md
index 93cb4c6..73b8ed5 100644
--- a/docs/ai-workflow-architecture.md
+++ b/docs/ai-workflow-architecture.md
@@ -74,3 +74,8 @@ claude.ai **설정 → 프로필 → 개인 선호** 한 줄:
 
 - **2026-08-16 v2.0** Codex 통합. v1.0의 "정본형에 DEV_STATE 금지" 판정 철회 (판정표 참조). FREEZE TAG 앵커 분기 사고 기록.
 - **2026-08-16 v1.0** 최초 작성 (3계층 모델, 아티팩트=대시보드 판정).
+
+## AGENTS.md 변경 이력 (AGENTS.md에서 이동)
+
+- **v2.0 (2026-08-16)** Codex 전역 설정과 통합. 정본형/단일형 2형태 구분을 폐지하고 표준 구조로 단일화 — 플라이 이스케이프에도 `DEV_STATE.md`가 재개 진입점이 됐다 (v1.0의 "정본형에 DEV_STATE 금지" 판정 철회. 사유: Codex가 인수인계를 역사 보관으로 정리하고 사용자가 승인). 크기 예산(8KiB/24KiB)·표준 시작 문구·원본-사본 버전 대조·앵커 단일 규칙 신설.
+- **v1.0 (2026-08-16)** Claude 초안.
