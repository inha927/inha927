# 공통 AI 협업 프로토콜

> 소유자: inha927 · v1.0 (2026-08-16)
> 적용 대상: 이 사용자와 작업하는 **모든 AI** (Claude, GPT/Codex, Cursor, 기타)
> 우선순위: **프로젝트 자체의 AGENTS.md > 이 공통 프로토콜 > AI 개인 메모리**

---

## 1. 정본(source of truth)은 항상 프로젝트 파일이다

대화 기록, 아티팩트/캔버스, AI의 개인 메모리는 정본이 아니다. AI 메모리는 기기마다·서비스마다 다르므로 **중요한 규칙과 상태는 반드시 프로젝트 파일에 남긴다.**

| 프로젝트 형태 | 판별 | 상태 정본 |
|---|---|---|
| **정본형** | 루트에 `AGENTS.md` + `Docs/` 폴더 | 그 체계의 문서 (인수인계·테스트 기록·실행 스펙) |
| **단일형** | 루트에 `DEV_STATE.md` | `DEV_STATE.md` |
| 둘 다 없음 | — | `DEV_STATE.md`를 만들어 시작한다 |

## 2. 세션은 읽기로 열고 쓰기로 닫는다

- **시작:** 상태 정본을 먼저 읽는다. 정본형이면 이 순서 — `AGENTS.md` → `Docs/README.md`(색인) → 인수인계 문서 **최신 절** → `테스트 기록.md` 최신 절.
- **종료:** 상태 정본을 갱신하고 끝낸다. 갱신 없이 끝내면 그 세션의 작업은 다음 AI에게 존재하지 않는 것과 같다.

## 3. 문서 소유권을 침범하지 않는다

- 실기 **PASS/FAIL은 테스트 기록 문서만** 소유한다.
- **실행하지 않은 검증은 어디에도 PASS로 적지 않는다.** `NOT_RUN`으로 적는다.
- 수치·게이트의 정본(예: 실행 스펙)의 **게임 규칙 변경은 사용자 승인 후에만** 한다.
- 기존 기록은 지우지 않는다 — 날짜 절(§)을 **추가**한다.

## 4. 사실과 추측을 구분한다

- 모든 주장에 표시한다: `[확인: 출처]` / `[추정]` / `[미확인]`
- 수치는 출처 없이 쓰지 않는다. 정보가 없으면 **없다고 보고한다. 지어내지 않는다.**
- 부분만 보고 전체를 단정하지 않는다 (목록·API는 전수 확인 후 "없다"고 판정).

## 5. 동시 작업 잠금

- `Docs/AI 작업 잠금/`에 `status: "ACTIVE"` 파일이 있으면 **그 영역은 다른 AI가 작업 중** — 건드리지 않는다.
- 큰 작업을 시작하면 잠금 파일을 만들고, 끝나면 해제한다.

## 6. 폐기 목록은 되살리지 않는다

상태 정본의 "실패한 접근"·"폐기" 목록에 있는 방법은 다시 시도하지 않는다. 되살릴 새로운 이유가 생겼다면 **이유를 먼저 사용자에게** 말하고 승인받는다.

## 7. 도구별 경계

- **UEFN 프로젝트 폴더**: git 커밋 금지 — 동기화는 UEFN **Revision Control**이 담당한다.
- 일반 git 저장소: 상태 갱신 후 상태 파일만 커밋·푸시한다.
- UEFN 작업은 **UEFN 기준**으로 답한다 (Verse·Creative 장치). 일반 UE5 문서 기준으로 답하지 않는다.

## 8. 어떤 문서에도 남기지 않는 것

비밀번호 · 토큰 · API 키 · 개인키 · 쿠키 · 결제정보.

## 9. 출력 형식

한국어 마크다운. 에디터 UI를 안내할 때는 **현재 에디터에 보이는 표시명(한글 우선)** 으로.

---

## 부록 — 이 사용자의 프로젝트 지도

| 프로젝트 | 형태 | 상태 정본 위치 |
|---|---|---|
| **플라이 이스케이프** (UEFN, 진행 중) | 정본형 | PC의 UEFN 프로젝트 폴더 (`AGENTS.md`+`Docs/`), Revision Control 동기화. claude.ai 아티팩트 "현황판"은 **읽기용 스냅숏일 뿐 정본 아님** |
| **FREEZE TAG** (UEFN, 기획 단계) | 전환 대기 | 현재 claude.ai 스킬 `freeze-tag-uefn`의 `project-state.md`. **UEFN 프로젝트가 생기는 날 플라이 이스케이프와 같은 정본형 구조로 이관** |
| **inha927/inha927** (GitHub) | 단일형 | 루트 `DEV_STATE.md`. 이 프로토콜의 보관처 |
