# 공통 AI 협업 프로토콜

> 소유자: inha927 · **v2.0 (2026-08-16)** — Claude v1.0 + Codex 전역 설정 통합본
> 적용 대상: 이 사용자와 작업하는 **모든 AI** (Claude, GPT/Codex, Cursor, 기타)
> **원본(정본)은 github.com/inha927/inha927 루트의 이 파일이다.** 사본(Codex 전역 `%USERPROFILE%\.codex\AGENTS.md`, PC `%USERPROFILE%\.claude\`, claude.ai 개인 설정)은 맨 위 버전으로 대조한다 — 버전이 다르면 원본을 따르고 사본을 갱신한다. **각 프로젝트 루트의 `AGENTS.md`는 사본이 아니라 독립 규칙이다** — 덮어쓰지 않는다.
> 우선순위: **프로젝트 자체의 AGENTS.md > 이 공통 프로토콜 > AI 개인 메모리**

---

## 1. 모든 프로젝트의 표준 구조

**정본(source of truth)은 항상 프로젝트 파일이다.** 대화 기록, 아티팩트/캔버스, AI의 개인 메모리는 정본이 아니다 — AI 메모리는 기기마다·서비스마다 다르므로, 중요한 규칙과 상태는 반드시 프로젝트 파일에 남긴다.

활성 프로젝트의 루트에는 파일 두 개가 있다. 없으면 만들고 시작한다.

| 파일 | 역할 | 크기 한도 |
|---|---|---|
| `AGENTS.md` | 그 프로젝트의 작업 규칙 | **24KiB** |
| `DEV_STATE.md` | 현재 상태·재개 스냅숏. **유일한 재개 진입점** | **8KiB** |

- 한도의 근거: Codex 기본 문서 로드 한도가 32KiB이고, 실측으로 확인된 로드는 두 파일 합계 약 21KB까지다 [확인: Codex 보고]. 한도(8+24=32KiB)를 꽉 채우면 로더 부가분 때문에 잘릴 수 있다 — **한도는 상한이지 목표가 아니다.** 잘려 읽히는 징후가 보이면 더 줄인다. 다른 도구의 한도는 [미확인].
- 심층 정본이 필요한 프로젝트는 `Docs/`를 둔다 (실행 스펙 · 테스트 기록 · 역사 보관). **세션 시작 시에는 읽지 않는다** — 필요할 때만 연다 (예외: 잠금 폴더 확인, §6). 폴더명 대소문자는 프로젝트 관례를 따른다 (이 레포는 `docs/`).
- **상태 앵커는 프로젝트당 정확히 1개다.** 앵커를 옮기면 옛 앵커 맨 위에 "→ 어디로 이관됨" 표지를 남긴다. 표지 없는 이동은 분기(fork)를 만든다 — 실제로 FREEZE TAG에서 앵커 2개가 서로 모순되는 사고가 났다 (부록 A).
- 여러 프로젝트의 상태를 섞는 공통 일지는 만들지 않는다. 임시·실험·생성물 폴더에도 상태 파일을 만들지 않는다.

## 2. 세션은 읽기로 열고 쓰기로 닫는다

**표준 시작 문구 — 프로젝트 파일에 접근 가능한 AI라면 이 한 줄이면 된다:**

> 프로젝트 루트의 `AGENTS.md`와 `DEV_STATE.md`를 읽고, 잠금을 확인한 뒤 DEV_STATE의 다음 단계부터 계속해.

- 시작 로드는 그 두 파일 + 잠금 폴더 확인(§6, 있으면)뿐이다. 나머지는 필요할 때 연다.
- 파일이 없으면 §1대로 만들고 시작한다 — `AGENTS.md`는 공통 프로토콜 포인터 한 줄이면 충분하고, `DEV_STATE.md`는 §3 형식 골격을 만든 뒤 "지금 목표"를 사용자에게 확인한다.
- 종료: `DEV_STATE.md`를 갱신하고 끝낸다. **갱신 없이 끝낸 세션의 작업은 다음 AI에게 존재하지 않는 것과 같다.**

## 3. DEV_STATE.md 표준 형식

```markdown
# DEV_STATE
> 갱신: YYYY-MM-DD HH:MM · 갱신자: <AI 이름/사람> · (git이면) 브랜치
## 지금 목표
## 완료된 것            (검증 방법까지)
## 핵심 결정과 이유      (왜 — 이게 제일 가치 있다)
## 핵심 파일            (경로 + 한 줄 역할)
## 게이트·테스트 현황    (정본 출처 명시. 실행 안 했으면 NOT_RUN)
## 알려진 문제
## 실패한 접근          (재시도 금지 목록)
## 다음 할 일           (첫 항목부터 바로 실행 가능하게)
```

- **DEV_STATE는 스냅숏이다** — 해결된 이슈·끝난 논의는 여기서 지운다. 단, 의미 있는 옛 상태는 삭제 대신 역사 문서(`Docs/역사/` 또는 날짜 파일)로 옮긴다. (기록성 문서의 보존 규칙은 §4 — 서로 다른 문서 얘기라 충돌이 아니다.)
- 8KiB를 넘기면 압축이 아니다 — 심층 문서로 빼라.

## 4. 값은 한 곳에만 산다 (문서 소유권)

- 실기 **PASS/FAIL의 정본은 테스트 기록 문서**다. **실행하지 않은 검증은 어디에도 PASS로 적지 않는다** — `NOT_RUN`.
- **수치·게이트 정의의 정본은 실행 스펙**이다. 게임 규칙 변경은 사용자 승인 후에만.
- `DEV_STATE.md`의 게이트·수치 표는 **사본(스냅숏)**이다. 정본 출처를 명시하고, 충돌하면 정본이 이긴다.
- 같은 값을 여러 문서에 복제하지 않는다. 다른 문서에는 참조만 남긴다.
- **기록성 문서(테스트 기록·실행 스펙·역사)의 기존 기록은 지우지 않는다** — 날짜 절(§)을 추가하거나 역사로 보관한다. (DEV_STATE 스냅숏의 정리 규칙은 §3.)

## 5. 사실과 추측을 구분한다

- 모든 주장에 표시한다: `[확인: 출처]` / `[추정]` / `[미확인]`
- **다른 AI의 보고도 출처다.** 직접 검증 전에는 `[확인: ○○ 보고]`로 적지, 사실로 승격하지 않는다.
- 수치는 출처 없이 쓰지 않는다. 정보가 없으면 **없다고 보고한다. 지어내지 않는다.**
- 부분만 보고 전체를 단정하지 않는다 (목록·API는 전수 확인 후에만 "없다" 판정).

## 6. 동시 작업 잠금

- `Docs/AI 작업 잠금/`(또는 프로젝트 관례 경로)에 `status: "ACTIVE"` 파일이 있으면 **그 영역은 다른 AI가 작업 중** — 건드리지 않는다.
- 큰 작업을 시작하면 잠금 파일을 만들고, 끝나면 해제한다.
- 잠금 파일 형식: 마크다운 1개, 파일명에 작업 영역, 본문에 `status: "ACTIVE"` · 시작 시각 · AI 이름. 해제 = `status: "DONE"`으로 바꾸거나 파일 삭제.

## 7. 폐기 목록은 되살리지 않는다

상태 정본의 "실패한 접근"·"폐기" 목록에 있는 방법은 다시 시도하지 않는다. 되살릴 새로운 이유가 생겼다면 **이유를 먼저 사용자에게** 말하고 승인받는다.

## 8. 도구별 경계

- **UEFN 프로젝트 루트: git 금지** — 동기화는 UEFN **Revision Control**이 담당한다. 같은 폴더에 git을 겹치지 않는다.
- 일반 git 저장소: 세션 마감 때 상태 파일 갱신을 반드시 커밋·푸시한다 (다른 기기·클라우드 세션이 읽도록). 작업 파일 커밋과 같이 담아도 된다 — 금지가 아니라 "상태 갱신을 빠뜨리지 마라"는 뜻이다.
- UEFN 작업은 **UEFN 기준**으로 답한다 (Verse·Creative 장치). 일반 UE5 문서 기준 금지.
- 각 도구의 문서 자동 로드 한도를 확인하고 §1의 크기 예산을 지킨다. 도구별 한도 상향(예: Codex `project_doc_max_bytes`)은 보조 수단이지 예산 초과의 면죄부가 아니다.

## 9. 어떤 문서에도 남기지 않는 것

비밀번호 · 토큰 · API 키 · 개인키 · 쿠키 · 결제정보.

## 10. 출력 형식

한국어 마크다운. 에디터 UI를 안내할 때는 **현재 에디터에 보이는 표시명(한글 우선)** 으로.

---

## 부록 A — 프로젝트 지도 (2026-08-16)

| 프로젝트 | 구조 | 상태 앵커 |
|---|---|---|
| **플라이 이스케이프** (UEFN, 진행 중) | 표준 구조 완료 [확인: Codex 보고] — 루트 `AGENTS.md`(13.1KB 압축) + `DEV_STATE.md`(7.6KB) + `Docs/`(스펙·테스트 기록·역사). 인수인계 문서들은 역사 보관으로 이동 | PC 프로젝트 루트 `DEV_STATE.md`. 동기화 = UEFN Revision Control. **[미확인] DEV_STATE.md가 Check-in Changes에 잡히고 다른 PC Sync Latest 후 복원되는지 — 확인 전까지 교차 PC 동기화 UNKNOWN.** claude.ai 아티팩트 "현황판"은 비정본 미러 (2026-08-15 스냅숏 고정, 자동 갱신 안 됨 [확인: Codex 보고]) |
| **FREEZE TAG** (UEFN, 기획 단계) | ⚠️ **앵커 분기 사고 — 사용자 판정 대기** | 후보 2개가 서로 모순된다: ① claude.ai 스킬 `project-state.md` (v3.1+v3.2, v3.2는 사용자 확정) ② Drive `FREEZETAG_04_프로젝트현황_LIVE_v4.md` (스스로 "최신 앵커" 선언, 매일 06:00 KST 자동 작업이 읽음 [확인: 해당 문서 자체 선언], v3.2 결정 미반영). **판정 전까지 어느 쪽도 정본으로 취급하지 않는다.** UEFN 프로젝트 생성일에 표준 구조로 이관 |
| **inha927/inha927** (GitHub, 공개) | 표준 구조 | 루트 `DEV_STATE.md`. 이 프로토콜의 원본 보관처. 공개 레포이므로 게임 기획 세부·수치 금지 |

지식 문서(상태 아님): claude.ai 스킬 `freeze-tag-uefn`의 uefn-knowledge · learning-sources, Drive `UEFN_Master_Reference.md`(2026-08-12). 뒤 둘은 역할이 겹친다 — 통합 후보.

## 부록 B — 사본 배포 지도

| 위치 | 소비자 | 갱신 방법 |
|---|---|---|
| **repo 루트 `AGENTS.md` (원본)** | 모두 | 여기부터 고친다. 고치면 버전을 올린다 |
| `%USERPROFILE%\.codex\AGENTS.md` | Codex 전역 | 원본 갱신 시 복사 |
| `%USERPROFILE%\.codex\config.toml` — `project_doc_max_bytes = 65536` | Codex 전역 (로드 한도 보조 설정) [확인: Codex 보고] | Codex 전용. 공통 규칙 아님 |
| PC `%USERPROFILE%\.claude\` (CLAUDE.md·commands) | 그 PC의 Claude Code 전역 | 원본 갱신 시 복사 |
| claude.ai 개인 설정 한 줄 | 같은 계정의 모든 Claude | "repo의 AGENTS.md를 따른다" 포인터라 갱신 불필요 |
| 각 프로젝트 루트 `AGENTS.md` | 그 프로젝트의 모든 AI | **사본 아님 — 독립 규칙.** 프로젝트 규칙이 공통 프로토콜에 우선. 버전 대조 대상이 아니다 |

## 변경 이력

- **v2.0 (2026-08-16)** Codex 전역 설정과 통합. 정본형/단일형 2형태 구분을 폐지하고 표준 구조로 단일화 — 플라이 이스케이프에도 `DEV_STATE.md`가 재개 진입점이 됐다 (v1.0의 "정본형에 DEV_STATE 금지" 판정 철회. 사유: Codex가 인수인계를 역사 보관으로 정리하고 사용자가 승인). 크기 예산(8KiB/24KiB)·표준 시작 문구·원본-사본 버전 대조·앵커 단일 규칙 신설.
- **v1.0 (2026-08-16)** Claude 초안.
